import "./stylesheets/main.css";

import './paths';
import './left-bar';
import './right-bar';


// // play silent audio to prevent background throttling?
// const context = new AudioContext()
// const source = context.createBufferSource()
// source.connect(context.destination)
// source.start(0)